# rust-claw environment example
#
# Quick start:
# 1) Copy this file: cp .env.example .env
# 2) Fill in REQUIRED values below.
# 3) Keep the rest as default at first.
#
# How config loading works:
# - OS environment variables are read first.
# - Missing keys are read from .env in this project folder.

# --------------------------
# Basic app settings
# --------------------------
ASSISTANT_NAME=Andy
MAIN_GROUP_FOLDER=main
DATA_DIR=./data

# --------------------------
# Runtime mode (recommended defaults)
# --------------------------
AGENT_RUNNER_MODE=container
TASK_EXECUTOR_MODE=agent
OUTBOUND_SENDER_MODE=discord

# --------------------------
# Timing (milliseconds)
# 1000 ms = 1 second
# --------------------------
POLL_INTERVAL_MS=2000
SCHEDULER_POLL_INTERVAL_MS=60000
IPC_POLL_INTERVAL_MS=1000
MAX_CONCURRENT_CONTAINERS=5

# Optional limits for SKILL.md injection
# SKILL_MAX_BYTES=12000
# SKILL_MAX_COUNT=8

# --------------------------
# Container runner (Docker + Codex app-server)
# --------------------------
CONTAINER_BINARY=docker
CONTAINER_IMAGE=rust-claw-codex-agent:latest
# CODEX_AUTH_DIR=~/.codex
CODEX_MODEL=gpt-5.2
CODEX_REASONING_EFFORT=medium
# Allowed values: low, medium, high, xhigh
# CODEX_APPROVAL_POLICY=never
# CODEX_SANDBOX_MODE=danger-full-access
# CODEX_ENABLE_SEARCH=true
# PROJECT_ROOT=.
# GROUPS_DIR=./groups
# CONTAINER_TIMEOUT_MS=1800000
# CONTAINER_SYSTEM_START_TIMEOUT_MS=30000

# Optional: pass selected credentials into agent containers
# Default: no credentials are passed through.
# If needed, define key values in .env and allowlist only the key names here.
# CONTAINER_ALLOWED_CREDENTIAL_KEYS_JSON=["MY_API_KEY"]

# --------------------------
# Discord (REQUIRED in discord mode)
# --------------------------
DISCORD_BOT_TOKEN=

# Main Discord channel to auto-register at startup (REQUIRED).
# Format: <discord_channel_id>@discord
# Example: 123456789012345678@discord
# If empty, register manually before use:
# cargo run -- bootstrap-main --jid <discord_channel_id>@discord
AUTO_REGISTER_MAIN_JID=

# Optional Discord behavior
# DISCORD_RECONNECT_DELAY_MS=5000
# DISCORD_PREFIX_OUTBOUND=false

# --------------------------
# Scheduler timezone (optional)
# If not set, system timezone is used.
# --------------------------
TIMEZONE=Asia/Seoul
